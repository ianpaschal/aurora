import State from "../../../src/core/State";

// Mock an engine
describe( "State", () => {
	test( "Accumulator should save left over time", () => {
		expect( 5 ).toBe( 5 );
	});
});

// Integration tests
test( "Accumulator should save left over time", () => {
	expect( 5 ).toBe( 5 );
});
